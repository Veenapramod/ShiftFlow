# Contributing to ShiftFlow

1. Create a focused branch: `git checkout -b feature/short-description`.
2. Keep commits small and descriptive.
3. Run `npm run check` and `npm run build` before opening a pull request.
4. Explain the problem, solution, and testing steps in the pull request.
5. Do not commit `.env` files, database files, credentials, or API keys.
